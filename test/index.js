//shorthand for module.exports.other, but you can only assign properties, not direct variable assignments(like functions)
exports.other = require("./myotherfile"); //we are already in ./test, so no need to pre-fix it
